export const metadata = {
	title: "Register",
	description: "Register your ENS and verify Aadhaar anonymously",
}

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
	return children
}
